{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
      // 这里可以覆盖或添加特定的编译器选项
      // 例如，如果你想要为 game 目录设置不同的输出目录
      "outDir": "../dist/game",
      // 或者指定不同的模块解析策略等
    },
    "include": [
      // 指定 game 目录下的 TypeScript 文件
      "./**/*.ts",
      "./**/*.tsx"
    ],
    "exclude": [
      // 排除不需要编译的文件或目录
      // 如果需要排除特定的测试文件或临时文件，可以在这里添加
    ]
  }
  